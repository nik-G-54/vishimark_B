# Port for Node server
PORT=8080

# Mongo connection string
MONGO_CONN=mongodb+srv://<user>:<pass>@cluster0.mongodb.net/<dbname>?retryWrites=true&w=majority

# Flask ML API verify endpoint
FLASK_URL=https://ml.yourdomain.com/api/verify

# Optional shared key if Flask requires it
ML_API_KEY=super-secret-key

# JWT and Cloudinary (if used by auth)
JWT_SECRET=change_me
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=